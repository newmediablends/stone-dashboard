# Tuesday, May 5, 2026

_Plan My Day not run yet — session log only. Trigger "plan my day" when ready for full structure._

---

## Section 3 — Log

- 08:00 — Lead Funnel pipeline diagnosis. backfillLeads returned 0 rows yesterday — diagnosed via Gmail MCP: forwarded leads have `from:lindon@musicologie.com` (not `noreply@mail.musicologie.app`), and most use legacy `*Field:*` format vs new `Field:` format. Parser query and field regex too narrow.
- 08:03 — Parser v2 → v7 evolution. Each iteration drove by inspecting actual sheet data:
  - v2: broaden Gmail search to include lindon@ forwards, dual-format field regex, best-effort original-date extraction from forwarded preamble
  - v3: silence reply-thread parse_errors (Gmail thread search bundles replies)
  - v4: allow multiple Fwd: prefixes (caught Vanessa McDonald — double-forwarded, was being dropped)
  - v5: tighten field-extractor regex (`\s*` → `[ \t]*` — empty fields were grabbing next line's content; instrument got "© 2026 Musicologie..." for blank rows)
  - v6: add PII columns M-T for owner review (explicit override of original "zero PII in Sheet" rule)
  - v7: column reorder for reading flow — received_at → contact → intent → child details → context → admin
- 08:30 — 31 leads cleanly ingested + Vanessa caught + clearLeadsData helper added so Mike can wipe + rebuild after schema changes
- 09:00 — Dashboard "SYNC FAILED" diagnosed. Root cause: Sheet was private, but adding PII created a conflict — making Sheet public would expose names/emails/phones to anyone who finds the dashboard URL.
- 09:15 — Decided on Apps Script Web App proxy. New separate project. doGet endpoint returns ONLY whitelisted non-PII columns as JSON. Sheet stays fully private.
- 09:45 — Mike deployed Web App, sent URL. Dashboard JS swapped from gviz CSV → Web App JSON. Tested endpoint — works, returns valid JSON with 31 leads.
- 10:00 — Dashboard lockdown. Passcode gate added to index.html (matches sensitivity.html / teacher-ops.html pattern: `139s` passcode, shared cookie unlocks all 24h). noindex meta + robots.txt for search privacy.
- 10:15 — Lead Funnel section now operational. Sheet private, dashboard private, PII boundary at Apps Script.

**Open Asks** (live session queue)
- [ ] Hard-refresh `newmediablends.github.io/musicologie-lindon-cm/` in ~1 min — confirm tiles populate (Today, 7-Day Avg, Top Source, Adult/Child, Conversion). If anything looks off, ping Stone.
- [ ] **Sheet age-band display** — child_age_band cells show "Mon Jun 10 2026" / "Sun Nov 15 2026" because Sheets auto-converts "6-10" / "11-15" to Date. Dashboard handles via normalizeAgeBand, but the Sheet itself is hard to read. Optional v8 parser fix: change age band values to date-proof format (`age_6_10`, `age_11_15`) — would require clearLeadsData + backfillLeads again. Defer until Mike says it's worth the round-trip.
- [ ] **2-hour rule reminder** — Julie owns call response via MyMusicology CRM. Dashboard is owner aggregate view only. No phone-ping needed for Mike per yesterday's drift correction.
- [ ] **IG Content Calendar — push to Google Sheets** — Full 4-week draft (May 5–Jun 1) saved at `1-Projects/Musicologie Studio 1 — Lindon Launch/IG-Content-Calendar-Draft.md`. 16 posts, 4 tabs ready (Calendar / Asset Checklist / Hashtag Bank / Strategy). Blocked on Google Docs MCP `invalid_grant`. **To resume:** reconnect at claude.ai → Settings → Integrations → Google Docs, then say "push the IG calendar to a sheet" and Stone creates it from the draft file. Also pending: IG announcement carousel + FB versions (Page + personal) drafted in chat — 3-slide carousel script ready, can be turned into actual graphics next session.

---

## End-of-Day Reflection

_TBD_
